{% extends 'base.html.twig' %}

{% block title %}Hello QuizzController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <ul>
                   <li><a class="btn btn-primary" href="#">{{ categorie.name }}</a></li>
                </ul>

                <ul>
                    {% for question in categorie.questions %}
                        <li>{{ question.question }}</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
